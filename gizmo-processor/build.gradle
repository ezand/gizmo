dependencies {
    compile project(':gizmo-core')
    compile project(':gizmo-ast')
    compile files('../lib/spi-0.2.7-SNAPSHOT.jar')

    if (isOSX()) {
        compile files("${System.properties['java.home']}/../lib/tools.jar")
    }
}